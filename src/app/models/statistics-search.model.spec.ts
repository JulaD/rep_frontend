import { StatisticsSearch } from './statistics-search.model';

describe('StatisticsSearch', () => {
  it('should create an instance', () => {
    expect(new StatisticsSearch()).toBeTruthy();
  });
});
